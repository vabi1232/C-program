#include <stdio.h>
void main()
{
	int a;
	int b;
	printf("nhap vao mot so a :", a);
	scanf("%d",&a);
	printf("nhap vao mot so b :", b);
	scanf("%d",&b);
	if (a%b==0)
	{printf("so a chia het cho so b");
	}
	else
	printf("so a khong chia het cho so b ");
}
